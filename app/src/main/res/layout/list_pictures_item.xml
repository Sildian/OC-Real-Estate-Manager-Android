<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="@dimen/picture_size_medium"
    android:layout_height="@dimen/picture_size_medium"
    android:layout_margin="@dimen/components_margin_medium"
    app:cardElevation="4dp">

    <ImageView
        android:id="@+id/list_pictures_item_image"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:contentDescription="@string/content_description_picture"
        app:srcCompat="@drawable/ic_picture_gray" />

    <ImageButton
        android:id="@+id/list_pictures_item_button_delete"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:contentDescription="@string/content_description_button"
        app:srcCompat="@drawable/ic_delete_black"
        android:layout_gravity="top|end"/>

    <ImageButton
        android:id="@+id/list_pictures_item_button_add_picture"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:contentDescription="@string/content_description_button"
        app:srcCompat="@drawable/ic_add_photo_black"
        android:layout_gravity="bottom|start"/>

    <ImageButton
        android:id="@+id/list_pictures_item_button_take_picture"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:contentDescription="@string/content_description_button"
        app:srcCompat="@drawable/ic_take_photo_black"
        android:layout_gravity="bottom|end"/>

</com.google.android.material.card.MaterialCardView>