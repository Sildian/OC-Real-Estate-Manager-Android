<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <group
        android:id="@+id/menu_toolbar_group">
        <item
            android:id="@+id/menu_toolbar_add"
            android:icon="@drawable/ic_add_white"
            app:showAsAction="ifRoom"
            android:title="@string/menu_toolbar_add">
        </item>
        <item
            android:id="@+id/menu_toolbar_edit"
            android:icon="@drawable/ic_edit_white"
            app:showAsAction="ifRoom"
            android:title="@string/menu_toolbar_edit">
        </item>
        <item
            android:id="@+id/menu_toolbar_search"
            android:icon="@drawable/ic_search_white"
            app:showAsAction="ifRoom"
            android:title="@string/menu_toolbar_search">
        </item>
    </group>
</menu>